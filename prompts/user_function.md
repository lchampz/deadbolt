## Module: {file} — you are looking at ONE function only

Function `{function}`, lines {start}-{end} of `{file}`.

```python
{source}
```

## Tests that mention this function or its module: {test_file}

```python
{tests}
```

List the blind spots inside `{function}` only. Use the line numbers shown, which
are the real line numbers in `{file}`.
